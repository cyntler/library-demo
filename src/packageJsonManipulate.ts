import { getConsumerProjectPaths } from './utils/getConsumerProjectPaths';

const { projectPackageJsonPath } = getConsumerProjectPaths();

export const addPackageJsonScripts = () => {};

export const removePackageJsonScripts = () => {};
